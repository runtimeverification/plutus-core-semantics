(program

(module Foo (import) (export () ())
  (define r1 [(lam x y y) 1 2])
  (define r2 [(lam x y z (builtin addInteger 10 z)) 1 2 3])
  (define r3 [(lam x y (builtin addInteger x y)) 10 20])
  (define r4 [(lam x y z (builtin addInteger x (builtin addInteger y z))) 10 20 5])
  (define r5 [[(lam f g [(lam x x) f]) (lam x y y) (lam x y x)] #A #B]))

)
