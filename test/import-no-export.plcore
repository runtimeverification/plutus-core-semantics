(program

(module Bar (import) (export () ())
  (declare r1 (integer))
  (define  r1 (builtin multiplyInteger 3 7))
)

(module Baz (import Bar Foo) (export () ())
  (declare r2 (integer))
  (define  r2 (builtin multiplyInteger 2 Bar.r1))
)

)
