(program

(module Foo (imported) (exported () ()) (local () ())

  test.preamble()

  ;; Testing allowance(Owner,Spender) when Owner = Spender = caller.

  test.allowance(Foo.caller, Foo.caller)

)

)
