(program

(module Foo (imported) (exported () ()) (local () ())

  test.preamble()

  // Testing transfer(To,Value) when To = caller and Value = BalanceFrom.

  test.transfer(Foo.caller, 70);  // balance of caller is 70

)

)
