(program

(module Foo (imported) (exported () ()) (local () ())

  test.preamble()

  // Testing approve(Spender,Allowance) when Spender = caller and Allowance = 0.
  // Might be useful in order to throw when transferring to yourself.

  test.approve(Foo.caller, 0)

)

)
