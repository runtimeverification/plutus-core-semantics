(program

(module Foo (imported) (exported () ()) (local () ())

  test.preamble()

  ;; Testing totalSupply() still works after changing it to a positive value.

  test.makeTotalSupply(737)
  test.totalSupply()

)

)
