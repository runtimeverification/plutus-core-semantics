(program

(module Foo (imported) (exported () ()) (local () ())

 test.preamble()

// Testing transferFrom(From,To,Value) when caller, From and To are distinct.
// Balance and Allowance are equal.

approve(9, 70);          // set allowance of caller (7) for 9
makeCaller(9)            // switch to 9
transferFrom(7, 5, 23);  // transfer 23 from 7 to 5

)

)
