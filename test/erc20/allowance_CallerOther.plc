(program

(module Foo (imported) (exported () ()) (local () ())

  test.preamble()

  ;; Testing allowance(Owner,Spender) when Owner = caller but Spender different.

  test.allowance(Foo.caller, 4)

)

)
