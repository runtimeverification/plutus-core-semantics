(program

(module Foo (imported) (exported () ()) (local () ())

  test.preamble()

  // Testing totalSupply() still works after changing it to zero.

  test.makeTotalSupply(0)
  test.totalSupply()

)

)
