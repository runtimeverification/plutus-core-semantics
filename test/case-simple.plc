(program

  (module SimpleCase (import) (export () (simpleCase nonExhaustive))

    (declare simpleCase (fun (integer) (integer)))
    (define simpleCase
      (lam x
        (case (builtin equalsInteger x 13)
          (Prelude.True  () 19)
          (Prelude.False () 23))))

    (declare nonExhaustive (fun (integer) (integer)))
    (define nonExhaustive
      (lam x
        (case (builtin equalsInteger x 13)
          (Prelude.True  () 19))))

  )

)
