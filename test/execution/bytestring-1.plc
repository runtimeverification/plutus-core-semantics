(program

(module Foo (import) (export () ())
(define b1
  (builtin takeByteString
   5 (builtin dropByteString
     1 (builtin concatenate
       #123456 #789ABC))))

(define b2
  (let
    (lam x (builtin concatenate x #1)) f
      [f #ABC]))
)

)
