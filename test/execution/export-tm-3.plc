(program

(module Foo (import) (export () (r1 r2))
  (declare r1 (integer))
  (define r1 (builtin addInteger 2 2))

  (declare r2 (integer))
  (define r2 3)
)

(module Bar (import Foo) (export () (r7))
  (declare r7 (integer))
  (define r7 (builtin multiplyInteger Foo.r1 7))
)

(module Baz (import Bar Foo) (export () ())
  (declare r8 (integer))
  (define r8 (builtin multiplyInteger Foo.r2 Bar.r7))
)

)
