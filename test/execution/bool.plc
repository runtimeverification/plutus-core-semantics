(program

(module Foo (import) (export () ())
  (define result
    (let
      (lam x
        (case x
          (Prelude.True  () (con Prelude.False))
          (Prelude.False () (con Prelude.True))))
        negate
        [negate [negate (con Prelude.True)]])))

)
