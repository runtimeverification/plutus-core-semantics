(program

(module Foo (imported) (exported () ()) (local () ())
  (define result
    (let
      (lam x
        (case x
          (Bool.True  () (con Bool.False))
          (Bool.False () (con Bool.True))))
        negate
        [negate [negate (con Bool.True)]])))

)
