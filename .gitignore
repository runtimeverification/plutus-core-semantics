/.build/*
!/.build/k
!/.build/iele

# Kompile generates these timestamped files, but does it clean them up?
# Maybe we should chdir to .build/ so that it leaves its junk there?
/.kompile*/
/.krun*/

# Files generated by the test framework
/test/*.iele

# Python / pytest caches
.pytest_cache/
__pycache__/
